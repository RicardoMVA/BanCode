<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.recode.bancode">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BanCode"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroActivity"/>
        <activity android:name=".ClienteActivity"/>
        <activity android:name=".EditarClienteActivity"/>
        <activity android:name=".EditarContaActivity"/>
        <activity android:name=".AdminActivity"/>
        <activity android:name=".ListaClientesActivity" />
        <activity android:name=".ListaContasActivity" />
        <activity android:name=".TransacoesActivity"/>
        <activity android:name=".ListaMovimentacoesActivity" />
        <!--    <activity android:name=".BoletoActivity" /> -->
    </application>

</manifest>